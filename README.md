Multicriteria Optimization Project

-[ ] Faire des tests en changeants de quelques jours les données pour voir si il y a des gros changements dans les résultats. Si c'est le cas, c'est nous avons un problème.
- Est ce qu'il faut centrer la matrice des rendements dans l'optimisation de portefeuille de Markowitz ?
- ACP

# Requirements
```
uv sync
uv pip install -e .
```


# Run Streamlit App
```
uv run streamlit run src/streamlit_tools/app.py
```